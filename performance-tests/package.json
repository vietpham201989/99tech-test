{
  "$schema": "https://json.schemastore.org/package.json",
  "scripts": {
    "test:performance": "k6 run --out json=./k6-results.json --http-debug=full",
    "test:performance:ci": "k6 run --out json=./k6-results.json"
  },
  "devDependencies": {
    "k6": "^0.0.0"
  }
}
